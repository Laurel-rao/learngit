github is a distrubute control system

learn to make code better

back to old version use command like 
	git reset --hard HEAD^
	git reset --hard HEAD~100  back 100 level
	git reset HEAD <file>

cancel the operation
	git reset --hard <version number>

